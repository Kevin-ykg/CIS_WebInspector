# P0～P3 重构验证状态

记录日期：2026-08-25

## 已确认

- 重构前 Debug x64 构建：成功，0 警告、0 错误。
- 重构前 Release x64 构建：成功，0 警告、0 错误。
- 重构后 Debug x64 构建：成功，0 警告、0 错误。
- 重构后 Release x64 构建：成功，0 警告、0 错误。
- `QrCodeDetector`、`ImageAligner`、`PatchDefectDetector` 的方法内容在物理拆分前后逐段文本一致。
- `MainViewModel` 方法数量保持一致；仅重命名两组采集缩放局部变量，并删除无执行内容的 PLC 注释占位分支。
- 回归快照导出和相同快照比较脚本已完成自检。

## 尚需人工运行确认

本轮没有自动操作 WPF 界面加载完整图库，因此以下内容不能仅凭构建结果宣称已经完成运行回归：

- 二维码文本、位置和跨帧识别结果；
- 拼接段起止位置与最终尺寸；
- 白墨等级、Mark 点和全局/非线性对准结果；
- 每个 PartId 的内部、外部和细线断裂结果；
- 完整检测耗时和长时间非托管内存趋势。

请使用同一图库、排版数据和 `app_config.json` 分别生成修改前后快照。P0～P3 的验收标准是算法结果不变；如果结果出现差异，应暂停后续重构并先定位差异来源。

## P4 配置快照补充

- P0～P3 离线业务回归已由用户确认通过。
- P4 不改变配置字段、默认值或算法参数，只把读取时机固定在数据源加载和检测作业提交边界。
- 算法服务已不再直接访问 `ConfigManager.Config`。
- P4 仍需用同一图库确认检测结果一致，并额外验证：离线暂停后修改参数不会改变已提交作业，重新加载图库后新参数能够生效。

## P5～P10 架构收敛补充

- 日志统一通过 `IAppLogger`，产品缺陷与工程处理异常使用独立状态表达；
- 段级结果增加稳定状态和原因码，中文日志不再承担机器接口语义；
- 在线/离线数据源由 `AcquisitionSession` 管理订阅、失败清理和释放；
- 检测作业由 `InspectionJobCoordinator` 管理串行、取消和过期结果屏障；
- 算法阈值、OpenCV 处理步骤及结果判定未调整。

仍需人工使用同一真实图库确认二维码、拼接、对准和三类缺陷结果一致。
